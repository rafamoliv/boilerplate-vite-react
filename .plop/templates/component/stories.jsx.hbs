import {{pascalCase name}} from './{{pascalCase name}}'

export default {
  component: {{pascalCase name}},
  title: 'Boilerplate/{{typeComponent}}/{{pascalCase name}}',
  args: {
    children: 'Hello world'
  }
}

const Template = (args) => <{{pascalCase name}} {...args} />

export const Default = Template.bind({})

export const {{pascalCase name}}Visible = Template.bind({})
{{pascalCase name}}Visible.parameters = {
  docs: {
    description: {
      story: 'Exemplo com segunda história com descrição'
    }
  }
}
{{pascalCase name}}Visible.args = {
  toggleContent: false
}
